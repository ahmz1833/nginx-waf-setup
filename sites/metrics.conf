server {
    listen 8880;
    server_name localhost;

    location /stub_status {
        stub_status;
        allow 127.0.0.1;
        # Allow Docker bridge / internal networks as needed
        allow 172.16.0.0/12;
        deny all;
    }
}
